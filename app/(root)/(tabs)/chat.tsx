import {Text} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

export default function Chat() {
    return (
        <SafeAreaView>
            <Text className="text-general-400">Chat</Text>
        </SafeAreaView>
    )
}